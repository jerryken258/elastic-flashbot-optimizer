[project]
name = "elastic-flashbot-optimizer"
authors = ["DevOps Team"]
description = "A decentralized arbitrage strategy management platform"
telemetry = false
requirements = []
[contracts.elastic-arbitrage]
path = "contracts/elastic-arbitrage.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
